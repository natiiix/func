(include stdio)

(func int main (int argc, char** argv)
    (if (> argc 1)
        (loop argc
            (printf "%s\n" argv[(- argc 1)])
            (-= argc 1)
        )
        (puts "Hello World!")
    )

    (return 0)
)
